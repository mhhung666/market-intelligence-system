# Docker Environment Configuration for Market Intelligence System

# Claude CLI Token (Required)
#
# Method 1: Using OAuth credentials (Recommended)
# Run: cat ~/.config/claude/credentials.json
# Copy the ENTIRE JSON content and paste it here (keep it on one line or use proper escaping)
#
# Method 2: Using just the access token
# From credentials.json, copy only the "accessToken" value
#
# Example (OAuth - full credentials):
# CLAUDE_TOKEN='{"claudeAiOauth":{"accessToken":"xxx","refreshToken":"xxx","scopes":["user:inference"],"subscriptionType":"pro"}}'
#
# Example (Simple - just access token):
# CLAUDE_TOKEN=your_access_token_here
#
CLAUDE_TOKEN=your_claude_token_or_full_json_here

# Timezone (Optional)
TZ=Asia/Taipei

# Optional: Ollama configuration if running locally
# OLLAMA_HOST=http://host.docker.internal:11434
