# 🚀 快速開始指南

## 📋 最常用的工作流程

### 方案 A: 完全自動化 (推薦) ⚡

```bash
# 1. 執行每日分析 (爬取 + 分析 + HTML)
make daily

# 2. 一鍵部署到 GitHub Pages
make deploy
```

**完成!** 🎉 1-2 分鐘後在 GitHub Pages 看到最新報告。

---

### 方案 B: 分步執行 (謹慎型)

```bash
# 1. 執行每日分析
make daily

# 2. 預覽檢查
make preview-pages  # 打開 http://localhost:8000

# 3. 確認無誤後部署
make deploy
```

---

### 方案 C: 只生成分析報告

```bash
# 只生成 markdown 報告 (會自動更新 HTML)
make analyze-daily

# 檢查生成的文件
ls -la analysis/market-analysis-*.md
ls -la analysis/holdings-analysis-*.md
ls -la docs/market.html
ls -la docs/holdings.html
```

---

## 🛠️ 常用指令速查

| 指令 | 說明 | 何時使用 |
|------|------|----------|
| `make daily` | 爬取數據 + 生成分析 + 更新 HTML | 每日例行作業 |
| `make deploy` | 提交 + 推送到 GitHub | 完成分析後部署 |
| `make preview-pages` | 本地預覽網站 | 確認格式正確 |
| `make update-pages` | 只更新 HTML | 修改 markdown 後 |
| `make commit-auto` | 自動提交 | 快速提交 |
| `make commit` | 互動式提交 | 自訂 commit message |
| `make push` | 推送到 GitHub | 提交後推送 |

---

## 📅 每日工作流程示例

### 早上 9:00 - 執行分析

```bash
cd ~/Desktop/MH/market-intelligence-system
make daily
```

**結果:**
- ✅ 爬取了最新市場數據
- ✅ 生成了 markdown 分析報告
- ✅ 自動轉換成 HTML
- ✅ 本地文件已準備好

### 上午 10:00 - 預覽檢查

```bash
make preview-pages
```

打開瀏覽器訪問 `http://localhost:8000`:
- 檢查市場分析頁面
- 檢查持倉分析頁面
- 確認格式無誤

### 上午 10:30 - 部署到線上

```bash
make deploy
```

**完成!** GitHub Pages 會在 1-2 分鐘內更新。

---

## 🔄 完整流程圖

```
┌──────────────────────────────────────────────┐
│ make daily                                    │
│ ├─ 爬取市場數據 (fetch-all)                  │
│ ├─ 生成 markdown 報告 (analyze-daily)        │
│ └─ 自動更新 HTML (update-pages)              │
└────────────────┬─────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────────┐
│ make preview-pages (可選)                    │
│ └─ 預覽檢查 http://localhost:8000            │
└────────────────┬─────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────────┐
│ make deploy                                   │
│ ├─ 更新 HTML (update-pages)                  │
│ ├─ 提交變更 (commit-auto)                    │
│ └─ 推送到 GitHub (push)                      │
└────────────────┬─────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────────┐
│ GitHub Pages 自動部署                        │
│ └─ 1-2 分鐘後可訪問最新報告                 │
└──────────────────────────────────────────────┘
```

---

## 🎯 特殊情況處理

### 情況 1: 只想更新 HTML (markdown 已存在)

```bash
make update-pages
```

### 情況 2: 只想提交不推送

```bash
make commit-auto  # 或 make commit
# 稍後再執行 make push
```

### 情況 3: 自訂 commit message

```bash
make commit
# 會提示: Enter commit message (or press Ctrl+C to cancel):
# 輸入你的訊息,例如: fix: 修正市場分析錯誤數據
```

### 情況 4: 重新生成報告 (覆蓋今日)

```bash
# 先刪除今日報告
rm analysis/market-analysis-$(date +%Y-%m-%d).md
rm analysis/holdings-analysis-$(date +%Y-%m-%d).md

# 重新生成
make analyze-daily
```

### 情況 5: 手動修改了 HTML,不想被覆蓋

```bash
# 在 run_daily_analysis_claude_cli.sh 中
# 暫時註釋掉 update_github_pages 調用

# 或者直接修改 docs/styles.css 而不是 HTML
```

---

## ⚙️ 環境設定 (一次性)

### 首次使用

1. **安裝 Claude CLI**
   ```bash
   npm install -g @anthropic-ai/claude-cli
   claude login
   ```

2. **安裝 Python 依賴**
   ```bash
   make install
   ```

3. **設定 Git**
   ```bash
   git config user.name "Your Name"
   git config user.email "your@email.com"
   ```

### 驗證設定

```bash
# 檢查 Claude CLI
claude --version

# 檢查 Python 環境
make test

# 檢查 Git
git status
```

---

## 🆘 常見問題

### Q: `make deploy` 失敗怎麼辦?

A: 檢查錯誤訊息:
- 如果是 `nothing to commit` - 正常,表示沒有變更
- 如果是 `authentication failed` - 檢查 GitHub 憑證
- 如果是 `rejected` - 先 `git pull` 再 `make push`

### Q: HTML 格式不對怎麼辦?

A:
```bash
# 重新生成 HTML
make update-pages

# 或手動調整 update_github_pages.sh 的 prompt
```

### Q: 忘記推送了怎麼辦?

A: 隨時可以推送:
```bash
make push
```

### Q: 想要回到之前的版本?

A: 使用 Git:
```bash
git log --oneline  # 查看歷史
git checkout <commit-hash> -- docs/  # 恢復特定版本
```

---

## 📚 更多資訊

- **完整文檔**: [GITHUB_PAGES_SETUP.md](GITHUB_PAGES_SETUP.md)
- **工作流程詳解**: [docs/WORKFLOW.md](docs/WORKFLOW.md)
- **Makefile 幫助**: `make help`

---

## 💡 最佳實踐

1. **每日固定時間執行** - 建議早上 9-10 點
2. **預覽再部署** - 使用 `make preview-pages` 檢查
3. **定期備份** - Git 會自動備份,但也可以定期 export
4. **監控 GitHub Pages** - 確認部署成功
5. **保持 Claude CLI 登入** - 定期執行 `claude login` 刷新

---

**就這麼簡單!** 🚀

兩行指令完成每日工作:
```bash
make daily
make deploy
```
