# GitHub Pages 部署指南

## 📋 概述

本專案已經配置好靜態 GitHub Pages 網站,包含兩個主要分析報告頁面:

1. **市場分析頁面** - 全球市場深度分析
2. **持倉分析頁面** - 個人投資組合分析

## 🎯 網站結構

```
docs/
├── index.html          # 首頁 (導航頁面)
├── market.html         # 市場分析報告
├── holdings.html       # 持倉分析報告
├── styles.css          # 共用樣式表 (深色主題)
├── .nojekyll          # GitHub Pages 配置
├── README.md          # 文檔說明
└── preview.sh         # 本地預覽腳本
```

## 🚀 部署步驟

### 1. 推送代碼到 GitHub

```bash
cd /home/kasm-user/Desktop/MH/market-intelligence-system

# 添加所有文件
git add docs/

# 提交
git commit -m "Add GitHub Pages static site for market intelligence reports"

# 推送到 GitHub
git push origin main
```

### 2. 在 GitHub 上啟用 GitHub Pages

1. 前往你的 GitHub repository 頁面
2. 點擊 **Settings** (設定)
3. 在左側菜單中找到 **Pages**
4. 在 **Source** 區域:
   - Branch: 選擇 `main`
   - Folder: 選擇 `/docs`
5. 點擊 **Save**

### 3. 等待部署完成

- GitHub 會自動部署,通常需要 1-2 分鐘
- 部署完成後,你會看到綠色的成功訊息
- 網站 URL 格式: `https://<你的GitHub用戶名>.github.io/<repository名稱>/`

## 🔍 本地預覽

在推送到 GitHub 之前,可以先在本地預覽:

### 方法 1: 使用腳本

```bash
cd docs
./preview.sh
```

然後在瀏覽器訪問 `http://localhost:8000`

### 方法 2: 手動啟動

```bash
cd docs
python3 -m http.server 8000
```

## 🔄 更新報告流程

當有新的分析報告時 (例如 2025-12-03):

### 選項 A: 手動更新 (簡單方法)

1. 將新的 markdown 報告轉換為 HTML (可以請 Claude 幫忙)
2. 替換 `docs/market.html` 或 `docs/holdings.html`
3. 更新首頁的日期
4. 提交並推送

```bash
git add docs/
git commit -m "Update reports for 2025-12-03"
git push origin main
```

### 選項 B: 自動化腳本 (建議)

創建一個轉換腳本:

```bash
# 在 market-intelligence-system/ 目錄下
./scripts/convert_to_html.py analysis/market-analysis-2025-12-03.md docs/market.html
./scripts/convert_to_html.py analysis/holdings-analysis-2025-12-03.md docs/holdings.html
```

## 📱 功能特色

### ✨ 響應式設計
- 手機、平板、桌面完美適配
- 流暢的導航體驗

### 🎨 深色主題
- 專業的深色配色
- 護眼舒適

### 📊 數據視覺化
- 美觀的表格樣式
- 顏色編碼的漲跌指標
- 清晰的層級結構

### 🔔 警示高亮
- 🚨 紅色警示框
- ⚠️ 黃色警告框
- 🟢 綠色成功框
- ⭐ 金色星級評分

### 🚀 性能優化
- 純靜態 HTML/CSS,加載快速
- 無需 JavaScript 框架
- SEO 友好

## 🛠️ 自定義配置

### 修改配色方案

編輯 `docs/styles.css` 中的 CSS 變量:

```css
:root {
    --primary-color: #2563eb;      /* 主色調 */
    --secondary-color: #0ea5e9;    /* 次要色調 */
    --success-color: #10b981;      /* 成功/上漲 */
    --warning-color: #f59e0b;      /* 警告 */
    --danger-color: #ef4444;       /* 危險/下跌 */
    --bg-color: #0f172a;          /* 背景色 */
    --card-bg: #1e293b;           /* 卡片背景 */
    --text-primary: #f1f5f9;      /* 主要文字 */
    --text-secondary: #94a3b8;    /* 次要文字 */
}
```

### 添加新頁面

1. 創建新的 HTML 文件 (例如 `docs/news.html`)
2. 使用相同的導航結構和樣式
3. 在所有頁面的導航欄中添加新頁面鏈接

## ❓ 常見問題

### Q: 網站顯示 404 錯誤?
A: 確認 GitHub Pages 設定中選擇了 `/docs` 資料夾,並且已經推送了所有文件。

### Q: 中文顯示亂碼?
A: 確認所有 HTML 文件的編碼為 UTF-8,並且包含 `<meta charset="UTF-8">`。

### Q: 樣式沒有生效?
A: 檢查 `styles.css` 的路徑是否正確,確保所有 HTML 文件都在同一目錄下。

### Q: 如何添加自定義域名?
A: 在 GitHub Pages 設定中的 "Custom domain" 欄位輸入你的域名,並在 DNS 設定中添加 CNAME 記錄。

## 📞 技術支援

如需幫助,請查看:
- [GitHub Pages 官方文檔](https://docs.github.com/en/pages)
- [HTML/CSS 基礎教學](https://developer.mozilla.org/zh-TW/)

---

**最後更新**: 2025-12-02
**版本**: v1.0
**製作**: Market Intelligence System
