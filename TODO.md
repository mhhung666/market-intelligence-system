# 📋 財報分析系統 - Version 3 改版計畫

> 微服務化架構 + 全自動化市場分析系統

---

## 🎯 Version 3 核心目標

打造一個微服務化的全自動市場分析系統，能夠:
1. **定時爬取資料** - 獨立的爬蟲服務 Docker 容器運行 cronjob
2. **智能分析** - 整合 Claude MCP Server 和本地 Ollama 雙引擎分析
3. **自動發布** - 生成 HTML 報告並自動提交到 GitHub Pages
4. **專案拆分** - 將爬蟲系統獨立為可重用的微服務

---

## 🏗️ 微服務架構規劃

### 專案結構重組

```
📦 market-data-crawler/          ← 新專案 (獨立爬蟲服務)
├── scrapers/
│   ├── common.py
│   ├── fetch_global_indices.py
│   ├── fetch_market_news.py
│   ├── fetch_holdings_prices.py
│   └── fetch_market_data.py
├── config/
│   └── indices.yaml
├── cron/
│   ├── asia-market.cron         # 08:00 GMT+8
│   └── us-market.cron           # 21:00 GMT+8
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
└── README.md

📦 financial-analysis-system/     ← 原專案 (核心分析系統)
├── analysis/                     # 保留所有分析報告
├── portfolio/                    # 保留投資組合管理
├── research/                     # 保留研究框架
├── templates/                    # 保留分析模板
├── tools/
│   ├── analyzers/                # 重構分析引擎
│   │   ├── claude_analyzer.py
│   │   ├── ollama_analyzer.py
│   │   └── analyzer_base.py
│   ├── report_generator/         # 新增 HTML 生成器
│   │   ├── templates/
│   │   ├── generator.py
│   │   └── markdown_to_html.py
│   └── utils/                    # 保留工具腳本
├── data/                         # 接收爬蟲數據
├── docs/                         # GitHub Pages 輸出目錄
├── docker-compose.yml            # 整合所有服務
├── Makefile
└── README.md
```

### 服務架構圖

```
┌──────────────────────────────────────────────────────────────┐
│                    Docker Compose 整合                        │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌────────────────────┐                                     │
│  │  crawler-service   │  ← 爬蟲微服務 (獨立專案)            │
│  │  - Cronjob 08:00   │                                     │
│  │  - Cronjob 21:00   │                                     │
│  └─────────┬──────────┘                                     │
│            │ volume mount                                    │
│            ▼                                                 │
│  ┌────────────────────┐                                     │
│  │   data/ 共享目錄    │                                     │
│  └─────────┬──────────┘                                     │
│            │                                                 │
│            ├────────────┐                                    │
│            ▼            ▼                                    │
│  ┌─────────────┐  ┌─────────────┐                          │
│  │  analyzer-  │  │  analyzer-  │                          │
│  │   claude    │  │   ollama    │                          │
│  └──────┬──────┘  └──────┬──────┘                          │
│         │                │                                   │
│         └────────┬───────┘                                   │
│                  ▼                                           │
│  ┌────────────────────────┐                                 │
│  │  report-generator      │                                 │
│  │  - Markdown → HTML     │                                 │
│  │  - Chart.js 圖表       │                                 │
│  └─────────┬──────────────┘                                 │
│            │ volume mount                                    │
│            ▼                                                 │
│  ┌────────────────────┐                                     │
│  │  docs/ (gh-pages)  │                                     │
│  └─────────┬──────────┘                                     │
│            │                                                 │
│            ▼                                                 │
│  ┌────────────────────┐                                     │
│  │  git-automation    │                                     │
│  │  - Auto commit     │                                     │
│  │  - Auto push       │                                     │
│  └────────────────────┘                                     │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

## 🔴 Phase 0: 專案拆分與重組 (Week 1) ⭐ 優先

> **目標**: 將爬蟲系統拆分為獨立的微服務專案

### 0.1 建立爬蟲獨立專案

- [ ] **建立 market-data-crawler 專案目錄**
  ```bash
  cd ~/Desktop
  mkdir market-data-crawler
  cd market-data-crawler
  git init
  ```

- [ ] **複製爬蟲核心檔案**
  - 從 `financial-analysis-system/tools/python/` 複製:
    - `scrapers/` 目錄 (所有 Python 爬蟲)
    - `config/indices.yaml`
    - `requirements.txt`
    - `tests/` 目錄 (測試檔案)

- [ ] **重構路徑依賴**
  - 修改 `scrapers/common.py` 的路徑邏輯
  - 將 `get_project_root()` 指向新專案根目錄
  - 更新輸出路徑為可配置 (環境變數)
  - 移除對原專案的硬編碼路徑

- [ ] **建立獨立文檔**
  - 撰寫新的 `README.md` (爬蟲專案文檔)
  - 說明如何獨立使用此爬蟲服務
  - 整合至其他專案的方法

### 0.2 更新原專案

- [ ] **清理原專案爬蟲程式碼**
  - 刪除 `tools/python/scrapers/` (已移至新專案)
  - 保留 `tools/utils/` (分析引擎)
  - 更新 `.gitignore`

- [ ] **建立新的分析引擎結構**
  ```
  tools/analyzers/
  ├── __init__.py
  ├── analyzer_base.py      # 抽象基類
  ├── claude_analyzer.py    # Claude 分析器
  └── ollama_analyzer.py    # Ollama 分析器
  ```

- [ ] **更新 Makefile**
  - 修改 `fetch-daily` 等指令調用 Docker 服務
  - 新增整合測試指令
  - 保持向後相容性

### 0.3 測試拆分結果

- [ ] **爬蟲專案獨立測試**
  - 在新專案中執行 `python scrapers/fetch_global_indices.py`
  - 驗證輸出正確
  - 確認無依賴原專案

- [ ] **原專案功能驗證**
  - 確認分析引擎仍可運作
  - 投資組合功能正常
  - INDEX.md 連結無誤

---

## 🔴 Phase 1: 爬蟲服務 Docker 化 (Week 1-2)

> **目標**: 將獨立的爬蟲專案 Docker 化並設定 cronjob

### 1.1 爬蟲專案 Dockerfile

- [ ] **建立 Dockerfile** (`market-data-crawler/Dockerfile`)
  ```dockerfile
  FROM python:3.11-slim

  # 安裝 cron 和基本工具
  RUN apt-get update && apt-get install -y \
      cron \
      tzdata \
      && rm -rf /var/lib/apt/lists/*

  # 設定時區
  ENV TZ=Asia/Taipei

  # 工作目錄
  WORKDIR /app

  # 安裝 Python 依賴
  COPY requirements.txt .
  RUN pip install --no-cache-dir -r requirements.txt

  # 複製爬蟲程式碼
  COPY scrapers/ ./scrapers/
  COPY config/ ./config/
  COPY cron/ ./cron/

  # 設定 cron
  RUN cat cron/asia-market.cron >> /etc/crontab
  RUN cat cron/us-market.cron >> /etc/crontab

  # 啟動 cron
  CMD ["cron", "-f"]
  ```

- [ ] **建立 crontab 設定檔**
  - `cron/asia-market.cron`:
    ```
    0 8 * * * root cd /app && python scrapers/fetch_global_indices.py -r 台灣 日本 香港 中國 韓國 > /proc/1/fd/1 2>&1
    ```
  - `cron/us-market.cron`:
    ```
    0 21 * * * root cd /app && python scrapers/fetch_global_indices.py -r 美國 歐洲 > /proc/1/fd/1 2>&1
    ```

- [ ] **建立 docker-compose.yml** (爬蟲專案)
  ```yaml
  version: '3.8'

  services:
    crawler:
      build: .
      container_name: market-data-crawler
      environment:
        - TZ=Asia/Taipei
        - OUTPUT_DIR=/app/output
      volumes:
        - ./output:/app/output        # 本地輸出目錄
      restart: unless-stopped
  ```

- [ ] **環境變數配置**
  - 建立 `.env.example`
  - 定義 `OUTPUT_DIR`, `TZ` 等變數
  - 文檔化所有環境變數

### 1.2 爬蟲程式碼重構

- [ ] **輸出路徑參數化**
  - `scrapers/common.py` 支援從環境變數讀取輸出路徑
  - 預設值設為 `/app/output`
  - 保持檔名規範 (`global-indices-YYYY-MM-DD.md`)

- [ ] **錯誤重試機制**
  - 網路錯誤自動重試 3 次
  - 單一市場失敗不影響其他市場
  - 詳細錯誤日誌

- [ ] **日誌輸出優化**
  - 使用 Python `logging` 模組
  - 分級日誌 (INFO, WARNING, ERROR)
  - 可選檔案輸出

- [ ] **資料驗證**
  - 檢查必要欄位存在
  - 價格數據合理性檢查
  - Schema 驗證 (可選)

### 1.3 測試與部署

- [ ] **本地測試**
  ```bash
  cd market-data-crawler
  docker-compose build
  docker-compose up -d
  docker-compose logs -f
  ```

- [ ] **驗證 cronjob 執行**
  - 手動觸發測試
  - 等待排程時間驗證
  - 檢查輸出檔案正確性

- [ ] **資源監控**
  - CPU 使用率
  - 記憶體使用
  - 磁碟空間

---

## 🟡 Phase 2: 主專案整合爬蟲服務 (Week 2-3)

> **目標**: 在原專案中整合爬蟲服務並建立分析引擎

### 2.1 主專案 Docker Compose 設定

- [ ] **建立主專案 docker-compose.yml**
  ```yaml
  version: '3.8'

  services:
    # 爬蟲服務 (引用獨立專案)
    crawler:
      build: ../market-data-crawler
      container_name: fas-crawler
      volumes:
        - ./data:/app/output        # 掛載到主專案 data/
      environment:
        - TZ=Asia/Taipei
      restart: unless-stopped

    # Claude 分析服務
    analyzer-claude:
      build:
        context: ./tools/analyzers
        dockerfile: Dockerfile.claude
      container_name: fas-analyzer-claude
      depends_on:
        - crawler
      volumes:
        - ./data:/app/data          # 讀取爬蟲數據
        - ./analysis:/app/analysis  # 輸出分析報告
      environment:
        - CLAUDE_API_KEY=${CLAUDE_API_KEY}
      restart: unless-stopped

    # Ollama 本地分析服務
    analyzer-ollama:
      image: ollama/ollama:latest
      container_name: fas-analyzer-ollama
      volumes:
        - ollama-data:/root/.ollama
      ports:
        - "11434:11434"
      restart: unless-stopped

  volumes:
    ollama-data:
  ```

- [ ] **環境變數管理**
  - 建立 `.env` 檔案 (不提交 git)
  - 建立 `.env.example` 範本
  - 包含: `CLAUDE_API_KEY`, `GITHUB_TOKEN` 等

- [ ] **網路與 Volume 設定**
  - 共享 `data/` 目錄
  - 分析服務可讀取爬蟲輸出
  - 報告服務可寫入 `docs/`

### 2.2 分析引擎重構

- [ ] **建立分析器基類** (`tools/analyzers/analyzer_base.py`)
  ```python
  from abc import ABC, abstractmethod

  class AnalyzerBase(ABC):
      @abstractmethod
      def analyze_market_data(self, data_path: str) -> str:
          """分析市場數據"""
          pass

      @abstractmethod
      def analyze_stock(self, ticker: str, data: dict) -> str:
          """分析個股"""
          pass
  ```

- [ ] **實作 Claude 分析器** (`claude_analyzer.py`)
  - 整合現有的 `analyze_daily_market.sh` 邏輯
  - 改為 Python 實作
  - 支援批次分析
  - Prompt 模板化

- [ ] **實作 Ollama 分析器** (`ollama_analyzer.py`)
  - 透過 Ollama API 調用本地模型
  - 負責初步資料篩選
  - 情緒分析與摘要生成

- [ ] **分析流程編排**
  - Ollama 預處理 → Claude 深度分析
  - 並行處理多個分析任務
  - 結果彙整與格式化

### 2.3 Makefile 更新

- [ ] **更新現有指令**
  ```makefile
  # 啟動所有服務
  up:
  	docker-compose up -d

  # 停止所有服務
  down:
  	docker-compose down

  # 查看日誌
  logs:
  	docker-compose logs -f

  # 執行每日流程 (透過 Docker)
  daily:
  	docker-compose exec analyzer-claude python /app/run_daily_analysis.py

  # 手動觸發爬蟲
  fetch-now:
  	docker-compose exec crawler python scrapers/fetch_global_indices.py
  ```

- [ ] **向後相容性**
  - 保留舊指令 (使用別名)
  - 提供遷移指南
  - 漸進式棄用警告

---

## 🟢 Phase 3: Ollama 整合與雙引擎分析 (Week 4-5)

> **目標**: 部署 Ollama 並建立雙引擎分析流程

### 3.1 Ollama 環境設定

- [ ] **選擇適合的模型**
  - 測試 `llama3.1:8b` (輕量級)
  - 測試 `qwen2.5:14b` (中文友好)
  - 測試 `mistral:7b` (速度優先)
  - 評估資源消耗與分析品質

- [ ] **Docker 內安裝 Ollama**
  - 使用官方 `ollama/ollama` image
  - GPU 支援設定 (如有)
  - 資源限制 (記憶體、CPU)

- [ ] **模型自動下載**
  ```bash
  docker-compose exec analyzer-ollama ollama pull llama3.1:8b
  ```

- [ ] **健康檢查**
  - API 可用性檢查
  - 模型載入狀態
  - 推論速度測試

### 3.2 分析任務分工設計

- [ ] **Ollama 負責的任務**
  - **大量資料初步篩選**: 從 100 條新聞篩選出 10 條重要的
  - **情緒分析**: 新聞正面/負面/中性分類
  - **關鍵字提取**: 識別重要關鍵字 (公司、產品、事件)
  - **快速摘要**: 生成 1-2 句話的新聞摘要

- [ ] **Claude 負責的任務**
  - **深度市場分析**: 整合多數據源的趨勢判斷
  - **投資建議**: 基於持倉的操作建議
  - **風險評估**: 識別潛在風險與機會
  - **結構化報告**: 生成完整的 Markdown 報告

- [ ] **流程串接**
  ```
  1. 爬蟲收集 100 條新聞
  2. Ollama 篩選出 10 條重要新聞 (本地免費)
  3. Claude 深度分析這 10 條新聞 (付費但精準)
  4. 生成最終報告
  ```

### 3.3 成本效益優化

- [ ] **API 調用統計**
  - 記錄 Claude API tokens 使用量
  - Ollama 本地推論次數
  - 成本估算

- [ ] **快取機制**
  - 相同數據避免重複分析
  - Prompt 結果快取 (24小時)

- [ ] **分析品質監控**
  - 人工抽樣檢查 (10%)
  - 自動化品質評分
  - A/B 測試不同 prompts

---

## 🔵 Phase 4: HTML 報告生成與 GitHub Pages (Week 6-7)

> **目標**: 自動生成 HTML 報告並發布到 GitHub Pages

### 4.1 報告生成器開發

- [ ] **建立報告生成器專案結構**
  ```
  tools/report_generator/
  ├── templates/
  │   ├── base.html           # 基礎模板
  │   ├── daily_report.html   # 每日報告
  │   ├── weekly_report.html  # 週報
  │   ├── stock_analysis.html # 個股分析
  │   └── portfolio.html      # 投資組合
  ├── static/
  │   ├── css/
  │   ├── js/
  │   └── images/
  ├── generator.py            # 主程式
  ├── markdown_parser.py      # Markdown 解析
  └── chart_builder.py        # 圖表生成
  ```

- [ ] **Markdown to HTML 轉換**
  - 使用 `markdown2` 或 `mistune`
  - 支援表格、程式碼區塊
  - 自訂擴展 (例如: 評級符號 ⭐)

- [ ] **Jinja2 模板引擎**
  - 建立響應式 HTML 模板
  - 支援深色/淺色主題切換
  - 手機版優化

- [ ] **Chart.js 圖表整合**
  - 持倉分布圓餅圖
  - 績效折線圖
  - 市場指數對比圖
  - 互動式圖表

### 4.2 靜態網站生成

- [ ] **首頁設計**
  - 最新市場分析 (今日報告)
  - 持倉概況卡片
  - 快速導航
  - 重要新聞摘要

- [ ] **分類頁面**
  - 按日期瀏覽 (每日/每週/每月)
  - 按公司瀏覽 (A-Z 排序)
  - 按產業瀏覽 (科技/金融/消費...)
  - 按評級瀏覽 (⭐⭐⭐⭐⭐ → ⭐)

- [ ] **搜尋功能**
  - 全文搜尋 (使用 Lunr.js)
  - 關鍵字高亮
  - 搜尋建議

- [ ] **RSS Feed**
  - 每日報告 RSS
  - 新分析 RSS
  - Atom feed 支援

### 4.3 Git 自動化

- [ ] **自動 commit 腳本** (`tools/git_automation/auto_commit.sh`)
  ```bash
  #!/bin/bash
  cd /app
  git add docs/
  git commit -m "docs: auto-update $(date +%Y-%m-%d) market analysis"
  git push origin main
  ```

- [ ] **智能 commit message**
  - 根據變更類型生成 message
  - `docs: add daily report 2025-11-28`
  - `feat: add new stock analysis AAPL`
  - `update: portfolio performance Q4 2025`

- [ ] **GitHub Pages 部署**
  - 推送到 `gh-pages` 分支
  - 或使用 `docs/` 目錄 (GitHub Pages 設定)
  - 自訂域名設定 (可選)

- [ ] **衝突處理**
  - 自動 pull before push
  - 衝突時發送通知
  - Fallback 機制

### 4.4 CI/CD Pipeline

- [ ] **建立 GitHub Actions** (`.github/workflows/deploy.yml`)
  ```yaml
  name: Deploy to GitHub Pages

  on:
    push:
      branches: [main]
      paths:
        - 'docs/**'

  jobs:
    deploy:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v3
        - name: Deploy to GitHub Pages
          uses: peaceiris/actions-gh-pages@v3
          with:
            github_token: ${{ secrets.GITHUB_TOKEN }}
            publish_dir: ./docs
  ```

- [ ] **自動測試**
  - HTML 語法驗證
  - 連結檢查 (內部連結)
  - 圖表渲染測試
  - 響應式設計測試

- [ ] **部署前檢查**
  - 檔案大小限制 (<1MB per file)
  - 圖片壓縮
  - CSS/JS minify

---

## 💡 Phase 5: 監控、日誌與優化 (Week 8+)

> **目標**: 建立完整的監控系統並持續優化

### 5.1 系統監控

- [ ] **爬蟲監控儀表板**
  - Prometheus + Grafana (可選)
  - 或簡易 Python Dashboard
  - 指標:
    - 執行次數 (成功/失敗)
    - 資料筆數
    - 執行時間
    - 錯誤率

- [ ] **分析任務監控**
  - Claude API 調用次數
  - Ollama 推論時間
  - Token 消耗統計
  - 分析品質評分

- [ ] **報告生成監控**
  - 生成時間
  - 檔案大小
  - 部署狀態
  - 訪問量 (GitHub Pages Analytics)

- [ ] **資源使用監控**
  - Docker 容器資源
  - 磁碟空間使用
  - 網路流量

### 5.2 日誌系統

- [ ] **集中式日誌收集**
  - 所有容器日誌統一收集
  - 使用 `docker-compose logs`
  - 或整合 ELK Stack (進階)

- [ ] **日誌分級與保存**
  - DEBUG: 開發時使用
  - INFO: 正常執行記錄
  - WARNING: 需注意的問題
  - ERROR: 錯誤記錄
  - 保存 30 天歷史日誌

- [ ] **錯誤追蹤**
  - 自動識別重複錯誤
  - 錯誤分類 (網路/API/邏輯)
  - 堆疊追蹤保存

### 5.3 通知機制

- [ ] **Email 通知**
  - 使用 `smtplib` 發送郵件
  - 執行成功日報 (每日 22:00)
  - 錯誤即時通知

- [ ] **Slack/Discord 整合** (可選)
  - Webhook 整合
  - 失敗警報
  - 每日摘要

- [ ] **異常警報規則**
  - 連續 3 次失敗 → 警報
  - API 錯誤率 > 10% → 警報
  - 磁碟空間 < 10% → 警報

### 5.4 效能優化

- [ ] **爬蟲平行化**
  - 使用 `concurrent.futures`
  - 多市場同時爬取
  - 控制併發數 (避免 rate limit)

- [ ] **快取機制**
  - Redis 快取 (可選)
  - 或簡易檔案快取
  - 快取 API 響應 (1小時)

- [ ] **資料庫索引優化** (如使用資料庫)
  - 日期欄位索引
  - ticker 欄位索引
  - 全文搜尋索引

- [ ] **Docker Image 瘦身**
  - 使用 multi-stage build
  - 移除不必要的依賴
  - 使用 `alpine` base image

---

## 📊 詳細實作時程

### Week 1: 專案拆分
- [ ] Day 1-2: 建立爬蟲獨立專案並複製檔案
- [ ] Day 3-4: 重構路徑依賴並測試
- [ ] Day 5-7: 更新原專案並驗證功能

### Week 2: Docker 化爬蟲
- [ ] Day 1-2: 建立 Dockerfile 和 docker-compose.yml
- [ ] Day 3-4: 設定 cronjob 並測試
- [ ] Day 5-7: 錯誤處理與日誌優化

### Week 3: 主專案整合
- [ ] Day 1-2: 建立主專案 docker-compose.yml
- [ ] Day 3-5: 重構分析引擎為 Python 服務
- [ ] Day 6-7: 整合測試

### Week 4: Ollama 部署
- [ ] Day 1-2: 部署 Ollama 並測試模型
- [ ] Day 3-5: 實作 Ollama 分析器
- [ ] Day 6-7: 雙引擎流程串接測試

### Week 5: 分析流程優化
- [ ] Day 1-3: 分析任務分工優化
- [ ] Day 4-5: 成本效益分析與調整
- [ ] Day 6-7: 品質監控機制

### Week 6: HTML 報告生成
- [ ] Day 1-3: 開發 Markdown to HTML 轉換器
- [ ] Day 4-5: 建立 Jinja2 模板
- [ ] Day 6-7: Chart.js 圖表整合

### Week 7: GitHub Pages 部署
- [ ] Day 1-2: 靜態網站生成
- [ ] Day 3-4: Git 自動化腳本
- [ ] Day 5-7: GitHub Actions CI/CD

### Week 8: 監控與優化
- [ ] Day 1-2: 監控儀表板
- [ ] Day 3-4: 日誌系統
- [ ] Day 5-7: 通知機制與效能優化

### Week 9+: 持續改進
- [ ] 根據實際運行調整
- [ ] 新增功能 (如需要)
- [ ] 文檔完善

---

## 🔗 技術棧詳細說明

### 爬蟲服務 (market-data-crawler)
```
語言: Python 3.11+
框架: 無 (輕量級腳本)
依賴:
  - yfinance >= 0.2.0      # Yahoo Finance API
  - pandas >= 2.0.0        # 資料處理
  - pyyaml >= 6.0          # 配置解析
  - requests >= 2.31.0     # HTTP 請求
部署: Docker + Cron
```

### 分析引擎
```
Claude 分析器:
  - anthropic Python SDK
  - Claude Sonnet 4 API

Ollama 分析器:
  - ollama Python SDK
  - 模型: llama3.1:8b / qwen2.5:14b

共用:
  - Python logging 模組
  - JSON schema 驗證
```

### 報告生成
```
模板引擎: Jinja2
Markdown 解析: markdown2 或 mistune
圖表: Chart.js
前端框架: 無 (純 HTML/CSS/JS)
樣式: Tailwind CSS (可選) 或自訂 CSS
```

### DevOps
```
容器化: Docker & Docker Compose
CI/CD: GitHub Actions
版本控制: Git
部署: GitHub Pages
監控: Prometheus + Grafana (可選)
日誌: Docker logs
```

---

## 📝 設計原則與最佳實踐

### 1. 微服務原則
- **單一職責**: 每個服務只做一件事
- **獨立部署**: 服務可獨立啟動與更新
- **鬆耦合**: 透過數據目錄或 API 通訊
- **高內聚**: 相關功能組織在一起

### 2. 自動化優先
- **最小化人工介入**: 所有流程自動化
- **Cron 排程**: 定時執行無需手動
- **錯誤自動恢復**: 重試機制
- **自動部署**: Git push 觸發部署

### 3. 可擴展性
- **易於新增數據源**: 透過 YAML 配置
- **易於新增分析類型**: 抽象基類設計
- **易於新增報告類型**: 模板化
- **易於新增通知渠道**: Plugin 架構

### 4. 容錯機制
- **單一失敗不影響整體**: 獨立的服務
- **自動重試**: 網路錯誤重試
- **優雅降級**: Claude 失敗時使用 Ollama
- **完整日誌**: 便於除錯

### 5. 成本效益
- **Ollama 本地預處理**: 減少 API 調用
- **Claude 深度分析**: 只用於關鍵決策
- **快取機制**: 避免重複運算
- **資源限制**: Docker 資源配額

### 6. 資料品質
- **Schema 驗證**: 確保資料格式正確
- **必要欄位檢查**: 避免空值
- **合理性檢查**: 價格、日期驗證
- **來源標記**: 追蹤資料來源

### 7. 版本控制
- **所有程式碼**: Git 版本控制
- **所有分析報告**: Markdown 提交 Git
- **所有配置**: YAML/ENV 版本控制
- **Semantic Versioning**: 語義化版本號

---

## 🎯 成功指標 (KPI)

### 系統穩定性
- [ ] **爬蟲成功率 >= 95%**
  - 監控 30 天內的執行成功率

- [ ] **分析任務成功率 >= 90%**
  - Claude API 可能偶爾失敗

- [ ] **報告生成成功率 >= 98%**
  - HTML 生成應該很穩定

- [ ] **系統連續運行 >= 30 天**
  - 無需人工干預

### 自動化程度
- [ ] **每日自動產出市場分析報告**
  - 早上 9:00 前完成

- [ ] **GitHub Pages 自動更新**
  - 報告產出後 10 分鐘內部署

- [ ] **零手動操作**
  - 除非系統出錯

### 成本控制
- [ ] **Claude API 月成本 < $50**
  - 透過 Ollama 預處理降低 API 調用

- [ ] **伺服器資源 <= 4GB RAM + 2 CPU**
  - Docker 資源限制

### 品質標準
- [ ] **分析報告有用性 >= 80%**
  - 人工抽樣評估

- [ ] **錯誤率 <= 5%**
  - 資料錯誤、分析錯誤

- [ ] **報告更新時效性 <= 1 小時**
  - 從資料爬取到報告發布

---

## 🚧 風險與挑戰

### 技術風險
1. **API 限制**
   - Yahoo Finance 可能限流
   - 解決: 降低頻率、增加重試間隔

2. **Claude API 成本**
   - Token 消耗可能超預算
   - 解決: Ollama 預處理、快取機制

3. **Docker 資源消耗**
   - Ollama 模型吃記憶體
   - 解決: 選擇小模型、資源限制

### 運維風險
1. **Cronjob 失效**
   - Cron daemon 可能故障
   - 解決: 健康檢查、監控告警

2. **磁碟空間不足**
   - 日誌和資料累積
   - 解決: 定期清理、壓縮歷史資料

3. **Git 衝突**
   - 多來源提交可能衝突
   - 解決: 自動合併、通知人工處理

### 業務風險
1. **資料源失效**
   - Yahoo Finance API 變更
   - 解決: 多數據源、監控 API 變更

2. **分析品質下降**
   - AI 幻覺、錯誤判斷
   - 解決: 人工抽查、品質評分機制

---

## 📚 參考資料與學習資源

### Docker
- [Docker 官方文檔](https://docs.docker.com/)
- [Docker Compose 參考](https://docs.docker.com/compose/)
- [Dockerfile 最佳實踐](https://docs.docker.com/develop/develop-images/dockerfile_best-practices/)

### Cron
- [Cron 表達式生成器](https://crontab.guru/)
- [Docker 中運行 Cron](https://www.docker.com/blog/how-to-use-the-official-nginx-docker-image/)

### Ollama
- [Ollama 官方網站](https://ollama.ai/)
- [Ollama Python SDK](https://github.com/ollama/ollama-python)
- [模型選擇指南](https://ollama.ai/library)

### Claude API
- [Anthropic API 文檔](https://docs.anthropic.com/)
- [Claude Prompt 工程](https://docs.anthropic.com/claude/docs/prompt-engineering)

### GitHub Pages
- [GitHub Pages 文檔](https://docs.github.com/en/pages)
- [GitHub Actions 教學](https://docs.github.com/en/actions)

---

## 📞 問題排查

### 常見問題 FAQ

**Q: 爬蟲 Docker 容器啟動後沒有執行 cron?**
A: 檢查:
1. `CMD ["cron", "-f"]` 是否正確
2. Crontab 檔案格式是否正確 (使用 `crontab -l` 檢查)
3. 容器日誌 `docker logs -f crawler`

**Q: Claude API 調用失敗?**
A: 檢查:
1. API key 是否正確設定在 `.env`
2. 網路連線是否正常
3. API 配額是否用盡

**Q: GitHub Pages 沒有更新?**
A: 檢查:
1. GitHub Actions 執行狀態
2. `docs/` 目錄是否有變更
3. GitHub Pages 設定是否正確

**Q: Ollama 推論很慢?**
A: 解決:
1. 使用更小的模型 (7B instead of 14B)
2. 增加 Docker 記憶體限制
3. 考慮 GPU 加速

---

## ✅ 檢查清單 (Checklist)

### Phase 0 完成標準
- [ ] 爬蟲專案可獨立運行
- [ ] 原專案功能無損
- [ ] 通過所有測試

### Phase 1 完成標準
- [ ] Docker 容器成功建置
- [ ] Cronjob 準時執行
- [ ] 資料正確輸出

### Phase 2 完成標準
- [ ] 所有服務整合成功
- [ ] 分析引擎運作正常
- [ ] Makefile 指令可用

### Phase 3 完成標準
- [ ] Ollama 模型部署成功
- [ ] 雙引擎分析流程正常
- [ ] 成本控制在預算內

### Phase 4 完成標準
- [ ] HTML 報告生成正確
- [ ] GitHub Pages 自動部署
- [ ] 網站可訪問

### Phase 5 完成標準
- [ ] 監控系統上線
- [ ] 日誌完整記錄
- [ ] 通知機制正常

---

## 🎉 下一步行動

### 立即開始 (本週)
1. **建立爬蟲獨立專案** → `cd ~/Desktop && mkdir market-data-crawler`
2. **複製核心檔案** → 從原專案複製爬蟲程式碼
3. **重構路徑依賴** → 修改 `common.py`

### 短期目標 (本月)
1. 完成 Phase 0 和 Phase 1
2. 爬蟲 Docker 服務上線
3. 測試 cronjob 自動執行

### 長期願景 (下季)
1. 完整微服務架構運行
2. GitHub Pages 自動發布
3. 穩定運行 90+ 天

---

**開始日期**: 2025-11-28
**目標完成**: 2026-01-31 (約 9 週)
**當前 Phase**: Phase 0 (專案拆分)
**下次檢視**: 每週一

---

*Version 3: 從單體應用到微服務化智能系統* 🚀
